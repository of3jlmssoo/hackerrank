#include <iostream>
#include <string>
using namespace std;

int main() {
	// Complete the program
        string first, second;
        cin >> first >> second;

        cout << first.size() << " " << second.size() << endl;
        cout << first+second << endl;

        auto fw = first[0];
        first[0] = second[0];
        second[0] = fw;

        cout << first<< " " << second << endl;

        return 0;
}